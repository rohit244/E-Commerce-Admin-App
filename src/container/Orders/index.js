import React from 'react'
import Layout from '../../components/layout'

const Orders = (props) => {
    return(
        <Layout sidebar>
            Orders
        </Layout>
    )
}

export default Orders